export enum FooterMode {
    ACTIVE_MODE, //button nav bar
    KIOSK_MODE, //application logo on the left
    PARAMETER_MENU, //"Paramétrage" and version number on the right + application logo on the left
}
